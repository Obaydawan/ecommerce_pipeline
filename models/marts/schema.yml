version: 2

models:
  - name: orders_summary
    description: "Summary of e-commerce orders with total item prices and freight values."
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      - name: order_status
        tests:
          - not_null
